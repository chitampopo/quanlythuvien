@model CoHoiCuoiCung.Models.QUYEN_SD

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>QUYEN_SD</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.MA_QUYEN)

        @Html.HiddenFor(model => model.MA_CHUC_VU)

        <div class="form-group">
            @Html.LabelFor(model => model.MENU, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.MENU)
                @Html.ValidationMessageFor(model => model.MENU)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Lưu" class="btn btn-default form-control" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
